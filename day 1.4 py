@@ -0,0 +1,16 @@
number=3
def isHappyNumber(number):
    reminder = 0
    sum = 0
    while (number!= 0):
        reminder = number % 10
        sum = sum + (reminder * reminder);
        number = number // 10
    return sum
result = number
